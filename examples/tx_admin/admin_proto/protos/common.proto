syntax = "proto3";

package admin.common;

/// 通用分页请求（page >= 1, size >= 1 且建议 <= 200）
message PageRequest {
  int64 page = 1;
  int64 size = 2;
}

/// 空响应
message Empty {}

/// 分页响应
message PageResponse {
  int64 total = 1;
  int64 page = 2;
  int64 size = 3;
}
