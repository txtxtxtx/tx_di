# 2026-08-15

## tx_admin DDD 战略重构收尾（完成）

- 继续并完成 `examples/tx_admin` 的 DDD 战略重构的测试验证阶段。
- 修复 admin_app 集成测试中的旧路径引用：`admin_domain::{user,role,menu,department,auth,config,dictionary,log,file}::` → 迁移到新边界路径（identity / system）。涉及 5 个文件：`tests/common/mod.rs`、`tests/real_db_test.rs`、`tests/workflow_test.rs`、`tests/user_test.rs`、`tests/auth_test.rs`。
- 修复 `auth_test.rs`：`AuthService::new(user_svc)` → `AuthService::new(user_repo)`（auth 域已改为依赖 `UserRepository` trait 而非 `UserService`）。
- 解决 Windows 构建产物损坏问题：`target/debug/deps` 中大量 0 字节 rmeta / 不兼容 PDB（LNK1207、E0786 invalid metadata、os error 1455 页面文件太小），用 `cargo clean -p tx_di_job -p tx_di_toasty -p admin_app` 清理后，用 `CARGO_BUILD_JOBS=2` 限制并行度重建成功。
- 全量测试通过：admin_domain 382、admin_macros 2、admin_app 多文件、admin_api 57，0 失败。

## 经验

- Windows 下并发 cargo 构建易触发页面文件不足（os error 1455）→ 残留 0 字节 rmeta / 损坏 PDB，导致 E0786/LNK1207。修复：`cargo clean -p <pkg>` + `CARGO_BUILD_JOBS=2`。
- PowerShell 的 `Remove-Item` 不能直接吃管道输入（报 InputObjectNotBound）；用 `cargo clean -p` 更可靠。

## tx_admin 配置修正（config.toml / config.production.toml）

- 各插件配置组件 `#[component(conf)]` 的实际 key 与旧注释/旧配置不一致，已修正节名：
  - `tx_di_file::FileConfig` → `conf = "file"`（原 `[file_config]` 错）；字段结构改为 `base_path/base_url/max_file_size/allowed_extensions/[[file.extra_storages]]`（无顶层 `backend`、`s3` 改嵌套在 extra_storages 元素内）
  - `tx_di_log::LogConfig` → `conf = "log"`（原 `[log_config]` 错）
  - `tx_di_toasty::ToastyConfig` → `conf = "toasty"`（原 `[toasty_config]` 错）
  - `tx_di_job::JobConfig` → `conf = "job"`（原 `[job_config]` 错）
  - `tx_di_sa_token::SaTokenConf` → `conf = "sa_token"`（不变）
  - `tx_di_axum::WebConfig` → `conf` 无参 → 默认 key `web_config`（camel_to_snake，不变）
  - `tx_di_nacos::RegistryConfig` → `[registry_config]`（非组件，bootstrap 读取，不变）
  - `[grpc_config]` 是 admin_api 自有自由 key（`AppAllConfig::get("grpc_config.port")`），非组件配置
- conf key 规则（tx-di-macros/codegen/factory.rs）：`conf = "x"` 用 x；`conf` 无参用 `camel_to_snake(结构体名)`。
- conf 组件取值：节存在→反序列化（类型错 panic）；节不存在→空表反序列化（走 Default）。故节名错误会**静默 fallback 默认值**，不报错。

## 环境变量机制（重要结论）

- **TOML 配置不支持 `${ENV_VAR}` 插值**。原 `password = "${NACOS_PASSWORD}"` 会被当作字面字符串，不会读环境变量。
- 真正支持的环境变量是代码 `std::env::var` 显式读取：`CONFIG_PATH`(选配置)、`GRPC_PORT`(gRPC 端口)、`APP_HOME`(锚定相对路径)、`DATABASE_URL`(覆盖 toasty)、`REDIS_URL`(覆盖 sa_token)、`SERVICE_IP`(nacos 注册 IP)。
- 若需 Nacos password 等字段支持环境变量，需扩展 tx_di_nacos 的 bootstrap 读取逻辑（仿 toasty 的 DATABASE_URL 兜底）。
