# 2026-08-10

## tx_admin 软件架构设计说明书

为 `examples/tx_admin` 编写了完整架构设计文档：`docs/09-软件架构设计说明书-从需求到SA模型.md`（约 1305 行）。

### 内容结构
- 需求空间分析（13 业务域 + 功能/质量属性 QAS/约束空间 → 模块追踪矩阵）
- 架构风格分析（分层+DDD / 管道-过滤器 / 事件驱动 / 插件-微内核 / 依赖注入 / 微服务）
- SA 模型确立（候选架构 C1-C4 权衡 → 决策 C3：单体分层+事件驱动+微服务预留；ADR-001~010）
- 四大阶段：设计（逻辑/进程/物理/数据/接口契约）、实现（模块规范/命名/DI 生命周期/质量门禁）、构建组装（Cargo workspace / linkme 注册 / app_loop! 装配）、部署（单服务多实例 / Nacos）
- 4+1 视图（逻辑/进程/开发/物理 + 4 个场景时序图，全 Mermaid）
- 质量属性架构级改进（Outbox、OpenTelemetry、API 版本、Job 分布式锁等）

### 撰写中核对的最新代码事实（关键）
- main.rs 已改用 `tx_di_nacos::app_loop!`（配置中心 + 服务注册 + 优雅重启），非旧文档的 BuildContext::new
- 新增 `tx_di_nacos` 插件（非组件 crate，基于 nacos-sdk 0.8）：配置拉取/深合并、watch 变更优雅重启、服务注册心跳
- `AdminPlugin` 的 app_async_init 注册 MetricsLayer(sort=5) + OperateLogLayer(sort=15) + HTTP/gRPC 路由 + gRPC 启动 + 事件订阅示例
- `admin_api/nacos.rs` 的 `AdminEndpoints` 通过 `register_endpoints` 注册 HTTP/gRPC 到 Nacos（SERVICE_IP 环境变量）
- 事件驱动已落地：聚合根 add_event → AppService 事务后 `DomainEventPublisher::publish` → EventBus（进程内，`as_trait = dyn DomainEventPublisher` 注入，订阅异常隔离）
- 密码已用 Argon2id（`password::hash_password`/`verify_password`），P2 已解决
- **M2/L7 分页问题已修复**：User `toasty_page!` 宏 SQL 层分页、Job `find_job_page` SQL 层分页，PROJECT.md 中的分页问题已过时
- DbInitPlugin：`init` 注册模型 + `app_async_init` 迁移(migrate_on_start)/种子
- gRPC 端口 50051，可用 `GRPC_PORT` 覆盖
- 配置路径 `examples/tx_admin/config/config.toml`（相对路径）

### 注意事项
- PROJECT.md 的"当前问题与待优化清单"部分（M2/L7 分页、P2 密码）已与代码不符，未来可更新
- 新增插件后必须在 `admin_api/src/lib.rs` 显式 `use` 触发 linkme 注册
