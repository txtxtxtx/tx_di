# 2026-07-04 工作日志

## tx-di-macros 宏模块结构重构
将单文件 `comp.rs`(703行) + `utils.rs` 按职责拆分为多模块结构：
- `attr/`（comp_attr.rs + field_attr.rs）
- `classify/fields.rs`（FieldKind 分类）
- `codegen/`（mod.rs 编排 + component_impl/factory/inner_init/meta_entry 四个生成器）
- `type_utils.rs` + `name_utils.rs`

改造原则：每个文件单一职责、derive_component_impl 变为 thin orchestrator、不改对外 API。
删除了死代码（clean_input/clean_fields 从未输出）、未使用的 strip_arc 函数。

验证：tx-di-macros 编译通过，tx-di-core 38 个测试全部通过（覆盖 inject/prototype/config/trait inject/interceptor/skip/custom expr 等所有宏路径）。
examples 中 tx_comp 错误为预先存在，与本次重构无关。
