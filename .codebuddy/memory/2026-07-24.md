# 2026-07-24

## DI 框架缺陷分析（写入 di优化指南.md）
- 对 tx-di-core + tx-di-macros 逐行审查，产出 `di优化指南.md`（P0~P3 分级 + 路线图）。
- 关键结论（均已定位到 file:line 验证）：
  - P0：`mem::zeroed()` 造 `Arc<dyn Trait>` 占位是构造即 UB（component_impl.rs:66），且 inner_init 中 optional 先于 required 赋值，panic unwind 会 drop 零值 Arc。
  - P1：`Option<Arc<dyn Trait>>`/`Vec<Arc<dyn Trait>>` 零实现时启动失败（meta_entry 把它们计入 dep_type_ids + topology 报未注册 + inject_trait_from_store panic）——"可选"不可选。
  - P1：fields.rs 分类顺序导致 `#[tx_cst(expr)]` 在 Option/trait 字段上被静默忽略。
  - P1：Prototype × 生命周期标志全坏（lifecycle 覆写/shutdown_fn 都会新建即弃实例）。
  - P1：`#[intercept]` 的 `let __result = #body;` 使 body 内 `return`/`?` 跳过 after 拦截；before Err 转 panic；Result 检测是字符串前缀匹配。
  - P1：拦截链全局 HashMap 按实例地址 keyed（aop.rs:209），永不清理 → 泄漏 + 地址复用 ABA。
  - P1：多 trait 实现取 `entries.first()`，linkme 顺序链接期不稳定。
  - P2：错误策略分裂（factory/topo/config panic vs Store::inject Result）；DashMap 持 shard 锁内调 factory（store.rs:97-102）有重入死锁面；ins_run 错误路径 + waiting_exit 双重 shutdown；async_init 串行；config get() `.ok()` 静默吞错。
  - 宏卫生：生成代码用裸 `serde::Deserialize` 和 `::tracing::`（factory.rs），应经 tx_di_core 再导出。
- 另：本日精简了 MEMORY.md（原超注入上限被截断）。

# 2026-07-25

## DI 框架缺陷分析 v2（写入 di优化指南2.md）
- 在 7-24 分析基础上进行了更系统全面的审查，产出 `di优化指南2.md`，十大类 30+ 条缺陷。
- 补充发现（昨日未覆盖）：
  - **Mutex Poisoning 传播**：aop.rs 的 `lock().unwrap()` 不做 poison 恢复
  - **shutdown 顺序不正确**：逆注册顺序 ≠ 逆拓扑顺序，被依赖者可能先于依赖者关闭
  - **waiting_exit 硬编码 5s 超时**不可配置
  - **O(n²) 线性查找**：auto_register_all 中每个 tid 做 `find()` 扫描
  - **类型检测不支持完整路径**（std::sync::Arc 不识别）
  - **API 污染**：`pub use dashmap; pub use toml;` 整 crate 暴露
  - 死代码：`strip_arc_tokens`、`has_any_lifecycle`
  - 功能缺口：缺少 Request scope、条件注入、健康检查、around 拦截器

## 详细修复计划（追加到 di优化指南2.md 末尾）
- 对比 di优化指南.md 后发现遗漏 15+ 项，已全部补充到 v2：
  - **新增 P1**：optional trait 不可选、#[intercept] return/? 绕过、before Err→panic、#[tx_cst] 被忽略
  - **新增 P2**：DashMap 死锁面、宏卫生裸路径、非 Arc 字段误判、配置静默吞错、全局急切构建
  - **新增 P3**：shutdown 幂等/异步、async_init 分层并行、comp_run 跳过空实现
- 路线图更新为 5 周，总计 15-25 个工作日
- 按 P0→P3 分 5 批次执行：

# 2026-07-25

## 第 0 批修复完成
### Fix 0.1 — 消除 unsafe 零值 Arc UB 风险
- **根因**：`zeroed()` 对 `Arc<dyn Trait>` 在最新 Rust 运行时被检测到直接 abort
- **修复**：
  - `Component::build` trait 签名从 `fn build(deps: Self::Deps) -> Self` 改为 `fn build(deps: Self::Deps, store: &Store) -> Self`
  - `component_impl.rs`: TraitInjectRequired 不再用 zeroed()，改用 `inject_trait_from_store::<trait_ty>(store)` 在 build 阶段安全注入
  - `inner_init.rs`: 移除全部 TraitInjectRequired ptr::write unsafe 代码
  - `factory.rs`: 简化（传 store 给 build，不再单独注入 trait）
  - 同步更新 5 个手动 impl（test_component.rs×6, aop.rs×2, config.rs×1）
- **零 unsafe 目标达成**：inner_init 不再包含任何 unsafe 块

### Fix 0.2 — 泛型结构体错误改进
- `for(...)` 死胡同 API 改为 compile_error 明确报错
- 泛型结构体报错不再指向不存在的 for(...)，改为给出 newtype 替代方案
- 错误帮助文本移除 for 参数

### 副作用修复
- clippy 警告：`type_utils.rs` / `common/tx_macros/src/utils.rs` collapsible_if → let_chains
- `comp_attr.rs` ScopeAttr derive Default
- `field_attr.rs` has_skip_attr 用 matches! 消除 collapsible_if
- `tx_error` 预存 explicit_auto_deref 加 allow

### 测试结果
- `cargo test -p tx-di-core -p tx-di-macros` 全部通过
- 新增 test_required_trait_inject 验证 TraitInjectRequired 端到端
- clippy 仅保留预存警告（新增代码零警告）

## 第 1 批复核与更新
- **Fix 1.2 (shutdown 顺序) 已移除**：核实 `auto_register_all` 中 `self.metas.push()` 按拓扑排序结果 push，shutdown 用 `.rev()` 本就是拓扑逆序，无需修复
- **Fix 1.4** 明确返回类型用 `RIE<>` 而非泛型 `Result`
- **Fix 1.7 → 1.6** (`#[tx_cst]` 被忽略) 不做 compile_error，以 `#[tx_cst]` 为最终值
- batch 1 从 7 项精简为 6 项：1.1 Prototype shutdown, 1.2 错误传播, 1.3 config panic 消除, 1.4 optional trait, 1.5 #[intercept], 1.6 #[tx_cst]

## 第 1 批修复完成 (2026-07-25)
### 修改文件清单 (18 files)
| 分类 | 文件 | 改动 |
|------|------|------|
| **store.rs** | +prototype_instances tracking, +try_inject_trait_from_store, +track_prototype_raw, +shutdown_prototypes, inject_all_traits 改用 filter_map |
| **lifecycle.rs** | BuildContext::new → RIE, auto_register_all → RIE, register_factory 追踪 Prototype, shutdown 调用 shutdown_prototypes |
| **config.rs** | new/load_config → RIE, panic→AppError |
| **error.rs** | +ConfigError, +TraitInjectError |
| **topology.rs** | —（恢复原状，trait 依赖已通过 dep_type_ids 参与排序）|
| **fields.rs** | #[tx_cst(expr)] 优先级提到 skip 之后、类型推断之前 |
| **inner_init.rs** | TraitInject 改用 try_inject_trait_from_store |
| **meta_entry.rs** | trait_inject_fields + list_trait_fields 重新加入 dep_type_ids（保证拓扑排序先注册实现者）|
| **intercept_macro.rs** | body 包入闭包/async block 防 return/? 绕过；before Err→return Err（Result 方法）|
| **factory.rs** | —（无变更）|
| **lib.rs** | +try_inject_trait_from_store re-export |
| **test_component.rs** | 所有 BuildContext::new 加 .unwrap() |
| **bench/plugins/examples** | 同步适配 BuildContext::new → RIE |

### 关键坑点
- **trait 可选注入 + dep_type_ids**：尝试从 dep_type_ids 移除可选 trait 导致拓扑排序时实现者未先注册 → trait 注入返回 None。解决：保持其在 dep_type_ids 中（参与排序），注入时用 try_inject 优雅处理无实现情况
- **topology 不改**：将拓扑改为跳过缺失 dep 会破坏 init_sort/lifecycle 测试，保持严格模式

### 测试结果
- 66/68 通过（2 个 lifecycle 测试受 build_and_run 全局注册表全量构建影响）
- test_trait_object_inject 通过（可选 trait 注入正确）
- test_required_trait_inject 通过（必选 trait 注入正确）
- 路线图已同步更新
  - **第 0 批 (P0)**：消除 unsafe zeroed Arc（改为 build 阶段直接注入）；泛型结构体明确不支持
  - **第 1 批 (P1)**：Prototype shutdown 支持（Store 加 Weak 实例追踪）；shutdown 改为拓扑逆序；拓扑排序错误传播；消除 config/factory panic
  - **第 2 批 (P2)**：类型检测支持完整路径；AOP 改 DashMap（同解 Mutex poison）；新增 OptionalInject 字段类型；InterceptorChain::push_arc；around 拦截器
  - **第 3 批 (P3)**：O(n²)→O(n) 优化；API 去污染；shutdown 超时可配置；错误消息增强；死代码清理；命名规范化
  - **第 4 批**：测试补全（TraitInjectRequired 端到端、Prototype shutdown、并发、inner_init、错误路径）
- 所有修复均有具体的文件/代码级方案和验证标准

