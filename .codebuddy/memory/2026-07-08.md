# 2026-07-08

## tx_di_can 迁移至 examples + A/B/C/D 全部完成
- 用户将 `tx_di_can` 从 `plugins/` 移到 `examples/tx_di_can`（示例代码目录，修复依赖后继续实现 A/B/C/D）。
- A XCP 标定 + A2L 解析（`src/xcp.rs`，含 SET_MTA/UPLOAD/DOWNLOAD/DAQ/ODT）。
- B 审计日志（`src/audit.rs`）+ HTML/PDF 报表（`src/report.rs`）。
- C CSV 离线分析（`src/record.rs` `analyze_csv` → `CsvAnalysis`）。
- D 中英 i18n（`src/i18n.ts`）+ .canproj 工程管理（`src/project.rs` `ProjectConfig`）。
- 前端新增 XcpView/AuditView/ProjectView，RecordReplayView 加离线分析，App.vue 加页签+语言切换。
- 验证：`cargo test -p tx_di_can` 89 passed/0 failed；`cargo check -p can-host` EXIT=0；`npx vue-tsc --noEmit` EXIT=0。
