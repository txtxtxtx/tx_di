# 2026-07-06

## tx_di_sip 插件实现与编译修复
- 完成 `tx_di_sip` 插件架构落地：`SipTx` 共享信封 + 中间件 DI 收集 + `SipPlugin`/`SipRouter`/`SipConfig`/`SipSender` 重写。
- 修复编译错误（用户"开始实现"→"先不考虑 gb28181"）：
  - `comp.rs`/`handler.rs`/`config.rs`：将不存在的 `#[tx_comp(...)]`/`CompInit` 改为 `#[derive(Component)] + #[component(...)]`，并补 `use tx_di_core::{Component, DepsTuple}`。
  - `comp.rs`：`shutdown` 必须是**模块级自由函数** `fn shutdown(_comp: &SipPlugin)`（首参为组件引用，不是 `&self`）—— 这是之前编译失败根因之一。
  - `sender.rs`：头文件 import 从 `rsipstack::sip::typed::{CallId, ContentType, Event, Expires, MaxForwards, ...}` 改为 `rsipstack::sip::{...}`（这些不在 typed 模块）。
  - `sender.rs`：`bye`/`cancel` 的 `anyhow::context(e)` 改回 `?` + `SipErr`（与 `register` 一致，RIE = Result<(), AppError>）。
  - `lib.rs`：doc 测试中 `recorder.lock().await.unwrap()` 改为 `*recorder.lock().await`（tokio Mutex 返回 guard）。
- `cargo build -p tx_di_sip` 与 `cargo test -p tx_di_sip` 均通过（34 单测 + doc-test 全绿）。
- 3 个含顶层 `.await` 的 doc 示例改为 `rust,ignore`（无法在 `fn main()` 编译）。
- 已更新 `MEMORY.md`：`shutdown` 回调签名、rsipstack 头文件位置 gotcha、tx_di_sip 架构摘要。

## gb28181 插件重设计（迭代1 + 迭代2）
- 用户意图：**全量重架构**（含 media/cascade/sdp/xml 模块重构）+ 认证**抽成 SipMiddleware 组件**（DI 收集、洋葱链前置）。
- 迭代1（编译迁移，已完成）：所有 `#[tx_comp(...)]`→`#[derive(Component)] + #[component(...)]`；handler 改 `SipTx`；出站改 `SipSender::send_message`/`dialog_layer()`；media `IE`→`AppError`。`cargo check -p tx_di_gb28181` 通过（仅 6 警告）。
- 迭代2（认证中间件化，已完成）：
  - 新建 `plugins/tx_di_gb28181/src/auth.rs`：`Gb28181AuthMiddleware`（字段 `Arc<Gb28181ServerConfig>` + `#[tx_cst(NonceStore::new())] nonce_store`），`#[derive(Component)] #[component(as_trait = dyn SipMiddleware)]`，`sort()=10` 最外层前置。
  - `process` 仅对 REGISTER 处理：ACL 前置（403）→ 无 Authorization 发 401 质询 → 有则 `verify_digest_auth` 校验（失败 403，成功放行）。非 REGISTER 直接 `next(tx)`。
  - `NonceStore` 从 `handlers.rs` 迁入 `auth.rs`；`handlers.rs::handle_register` 移除摘要认证与 ACL，仅留注册/注销业务；`register_server_handlers` 去掉 nonce 传递。
  - `Gb28181Server` 删除 `nonce_store` 字段；清理迭代1 两个警告（`plugin_tail.rs` 多余 `Arc` import、`plugin.rs` 多余 `nonce_store` clone）。
  - 编译通过：剩余 4 警告均为 media 模块**既有**问题（mediamtx `port` 未读、zlm 字段非 snake_case），非本次引入。
- 待做（迭代3+）：真 BYE（`SessionInfo` 存 `ClientInviteDialog` + `SipSender::bye`）；sdp/xml 模块重构；cascade/media 对齐收尾。
